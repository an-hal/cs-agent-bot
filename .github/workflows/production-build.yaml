# name: Production Build

# on:
#   release:
#     types:
#       - published

# env:
#   IMAGE: ${{ vars.ARTIFACT_REGISTRY_CONTAINER }}/${{ vars.IMAGE_NAME }}

# jobs:
#   check-target-commit:
#     uses: Sejutacita/shared-gh-workflow/.github/workflows/check-prod-target-branch.yaml@master
#     with:
#       target_commitish: ${{ github.event.release.target_commitish }}
#       tag_name: ${{ github.event.release.tag_name }}

#   build:
#     needs: [check-target-commit]
#     name: Build Image
#     runs-on: [self-hosted, backend-prioritized]
#     if: github.event.release.target_commitish == 'master'
#     steps:
#       - name: Check out repo
#         uses: actions/checkout@v4
#       - name: Build image
#         run: >
#           gcloud builds submit --config ./build/cloudbuild.yaml
#           --gcs-log-dir gs://cloudbuild-sejutacita-id/logs
#           --gcs-source-staging-dir gs://cloudbuild-sejutacita-id/source
#           --substitutions _IMAGE_TAG=${{ env.IMAGE }}:${{ github.event.release.tag_name }},_IMAGE_TAG_LATEST=${{ env.IMAGE }}:latest
