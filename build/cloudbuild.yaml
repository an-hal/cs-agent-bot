steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-f",
        "build/Dockerfile",
        "-t",
        "${_IMAGE_TAG}",
        "-t",
        "${_IMAGE_TAG_LATEST}",
        ".",
      ]
images:
  - "${_IMAGE_TAG}"
  - "${_IMAGE_TAG_LATEST}"
